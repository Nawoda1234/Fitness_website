import React from "react";
import "./globals.css";

export const metadata = {
  title: "Contact Us",
  description: "Contact form with info boxes",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
